name: AI Core Governance P0 Shadow

on:
  pull_request:
    paths:
      - 'contracts/ai-core/governance-p0.shadow.json'
      - 'scripts/check-ai-core-governance-shadow.mjs'
      - '.github/workflows/ai-core-governance-shadow.yml'
  push:
    branches: [main]
    paths:
      - 'contracts/ai-core/governance-p0.shadow.json'
      - 'scripts/check-ai-core-governance-shadow.mjs'
      - '.github/workflows/ai-core-governance-shadow.yml'

permissions:
  contents: read

jobs:
  parity:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/check-ai-core-governance-shadow.mjs
