# New freepasserp.com v1 Firebase project only. Never copy legacy ERP values.
FREEPASS_FIREBASE_PROJECT_ID=
FREEPASS_FIREBASE_CLIENT_EMAIL=
FREEPASS_FIREBASE_PRIVATE_KEY=

# Browser Firebase configuration is intentionally deferred until the new
# project and authentication provider are approved.
