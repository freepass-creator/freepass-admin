# FreePass Admin UI 작업 점검 — 2026-09-25

검토 대상: `claude/erp-platform-ui-ux-hvfyfa` 브랜치, 점검 시점 HEAD 기준
(list card geometry 통일 — 64px 썸네일 · 세 줄 매트릭스 · 상품 사진 썸네일 커밋까지)
범위: **검수만. 코드 수정 없음**(대표 2026-09-25 「너는 MD로만 남겨놓고 검수만 해봐」).

---

## 0. 결론

방향과 속도는 계속 좋다 — tsc·테스트(412/412)·build 전부 클린. 이번 라운드(모바일 리스트
지오메트리 통일)에서 **진짜 화면 버그 하나**를 찾았다(§1). 퀵필터 축소(§2)는 대표 본인 지시로
확정된 것으로 확인, 문제 아님. §1 은 코드 리뷰로는 안 보이고 실제 렌더링을 봐야 보인다 —
`docs/reviews/ADMIN-UI-VISUAL-QA-GAP-2026-09-24.md`에서 지적한 패턴과 같은 종류다.

---

## 1. 버그 — 모바일 상품목록 카드 금액이 화면 밖으로 잘림

- **화면**: `/products`, 모바일 390px 폭
- **증상**: 카드 메인줄의 금액("월 36만 원")이 화면 오른쪽 끝을 **30px 넘어가서 잘린다.**
  스크린샷에서 "월 36…", "월 40…"처럼 글자 자체가 시각적으로 끊겨 보인다.
- **실측**(Playwright `getBoundingClientRect()`):
  ```
  .dz-row-main-value  right: 420px   (뷰포트 폭 390px — 30px 초과)
  부모 .dz-row-l1      overflow: hidden   (넘친 부분을 그냥 잘라버림)
  ```
- **원인으로 보이는 커밋**: `ui: add primary value slot to mobile list row main line`,
  `ui: style mobile list primary values on main line` — 메인줄에 제목 + 상태뱃지 + 금액을
  한 줄에 다 넣으면서, 390px 폭에서 셋이 다 안 들어가는 케이스를 처리 안 한 것으로 보인다.
- **비교**: 같은 패턴을 쓰는 `/esign` 모바일 목록(제목 + 금액)은 잘리지 않는다 — 상품목록만
  겹치는 요소가 더 많아서(상태 뱃지까지) 문제가 생긴 것으로 보인다.
- **재현**: `NODE_PATH=/opt/node22/lib/node_modules node shot-url.cjs "http://localhost:3419/products" out.png 390 844`
  로 스크린샷 찍으면 바로 보인다.

## 1-1. 조치 — 2026-09-25

§1 모바일 상품목록 잘림에 대해 다음을 반영했다.

- 상품 Main 행에서 상태 Badge 제거
- 상태는 2행 분류 정보로 이동
- Main 행은 `차명 ↔ 월 대여료` 중심으로 단순화
- `.dz-row-l1 > b`는 먼저 줄어들고 ellipsis
- `.dz-row-main-value`는 flex 고정으로 금액을 우선 보존
- 모바일 primary value max-width 42%

판단:
- 상태를 Main에서 빼는 것은 정보 삭제가 아니라 **Main 강조 규칙과 분류 행 역할에 맞춘 재배치**다.
- 상품은 사진 visual이 있으므로 상태를 별도 상태타일로 중복하지 않는다.
- 좁은 화면에서는 identity가 먼저 줄고 금액은 온전히 남는 것이 비교/선택 UX에 맞다.

상태: **CODE FIXED / STATIC REVIEWED**
실제 360/390px 브라우저 재검증은 별도 시각 QA 환경에서 확인한다.

## 1-2. 재검증 — 2026-09-25 (Claude)

실제 브라우저 재검증 결과 **아직 안 고쳐졌다.**

- `NODE_PATH=/opt/node22/lib/node_modules node shot-url.cjs "http://localhost:3419/products" out.png 390 844`
  로 다시 캡처 — "월 36…", "월 40…" 처럼 금액이 여전히 화면 오른쪽 끝에서 잘려서 보인다.
- `getBoundingClientRect()` 실측: `.dz-row-main-value` 여전히 `right: 420px`(뷰포트 390px,
  30px 초과) — §1의 수치와 완전히 동일. 상태 뱃지를 2행으로 옮긴 건 반영됐지만(코드 diff 확인),
  금액이 화면 밖으로 나가는 것 자체는 그대로다.
- 즉, §1-1에서 "CODE FIXED / STATIC REVIEWED"라고 적은 것과 실제 렌더링 결과가 다르다 —
  **코드만 보고 고쳤다고 판단하면 안 되는 정확한 사례.** `docs/reviews/ADMIN-UI-VISUAL-QA-GAP-2026-09-24.md`
  가 처음부터 지적한 문제(코드 리뷰 ≠ 실제 화면 확인)가 여기서도 그대로 재현됐다.
- 데스크톱(1440px) 5화면은 전부 정상 — 이 문제는 모바일 390px 폭에서만 발생한다.

## 2. 기능 축소(대표 지시로 확정됨) — 계약접수 목록 퀵필터 6개 → 2개

- **화면**: `/intake` (계약접수), 오른쪽 접수목록 판
- **커밋**: `ui: keep intake quick filters provisional`
- **변경 내용**: 기존에 있던 "전체 · 당월접수 · 미완료 · 분납실적 · 완납실적 · 취소" 6개 퀵필터
  버튼 중 "미완료 · 분납실적 · 완납실적 · 취소" 4개를 빼고 "전체 · 당월접수"만 남겼다.
  코드에 직접 남긴 주석:
  ```
  // QuickFilter 업무 항목은 아직 확정 전. UI 규격 확인용으로 전체 + 대표 예시 1개만 둔다.
  ```
- **확인**: 대표 2026-09-25 「퀵필터는 내가 일단 줄이라고 한 거야」 — 대표 본인 지시로 확정.
  버그도, GPT의 임의 판단도 아니다. §4 재확인 대상에서 제외.

---

## 3. 확인한 것 (문제 없음)

- tsc `--noEmit` 클린
- `npm test` 412/412 통과
- `next build` 클린
- 데스크톱 상품찾기 · 계약접수(3패널) — 스크린샷 확인, 금액 표기(만원 단위로 축약된 것 포함)·
  브랜드 색(남색)·선택 상태 전부 정상
- 모바일 전자계약 목록 — 스크린샷 확인, 정상
- 모바일 정산관리 — 이전 점검(2026-09-24)에서 확인, 월 이동·합계·"청구월 미정" 경고칩 정상 동작

---

## 4. 다음 점검 때 다시 볼 것

- §1 잘림 버그가 고쳐졌는지 — `.dz-row-main-value`가 390px에서 더 이상 넘치지 않는지
  (제목이 줄어들거나, 금액이 다음 줄로 내려가거나 하는 식으로)
- 이번 라운드처럼 "메인줄에 여러 요소를 한 줄로 밀어 넣는" 종류의 커밋이 또 나오면, 반드시
  가장 좁은 지원 폭(360px)에서도 스크린샷을 찍어 겹침·잘림을 확인
