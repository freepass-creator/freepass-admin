/** ★★두 가지를 «가른다» — 방식(구조)과 테마(껍데기) (2026-09-06)
 *
 *  대표: 「일단은 **테마만 바뀌는 게** 있어. 그냥 레트로 그런 방식의 그런 테마만 바뀌는 거.
 *         그리고 **아예 이 테마를 바꿔버리면 «구조 자체가» 바뀌는 게** 있어.
 *         그래서 **테마와 그 방식을 두 개를 만들어야** 될 거 같아.
 *         ★테마는 그냥 «껍데기» 가 바뀌는 거. 그리고 아예 «구조 자체» —
 *         앱의 관리 ERP 구조 자체가 바뀌는 게 있고」
 *
 *  ★맞다. 전에는 이 둘을 «한 목록» 에 섞어 뒀다 —
 *    「고전모드 · 요즘모드」 옆에 「하루할일(본체)」 이 같이 있었다.
 *    ★그런데 본체는 «화면 구성이 다른 물건» 이다(전 주석에도 그렇게 적혀 있었다).
 *      껍데기를 갈아 끼우는 것과 구조를 통째로 바꾸는 것을 한 줄에 두면,
 *      누르는 사람은 「같은 급의 선택」 으로 읽는다. 그러다 구조가 통째로 바뀐다.
 *
 *  ┌─ 방식 (구조) ─ ★둘뿐이다. 「일을 어떻게 만나나」 가 갈린다. 웹에서만 갈린다
 *  │    고전식   ★능동형 — «내가 찾아 들어간다». 왼쪽 메뉴에서 골라 창을 띄우고 목록을 판다
 *  │    현대식   ★할 일을 «내려주는» 것 — 일이 나에게 온다. 기존에 만들어 둔 JPK워크가 그것
 *  └─ 테마 (껍데기) ─ ★여럿이다. 자리는 «그대로», 결만 바뀐다
 *       레트로 · 요즘 · (앞으로) 다크 · 간편 …
 *
 *  ★★대표(2026-09-06): 「테마는 되게 «여러 가지» 가 있을 거고, 구조는 그냥 고전·현대
 *    «이렇게 두 개» 가 있겠지. 테마는 뭐 다크모드·고전모드·현대모드·간편모드 —
 *    이렇게 «색깔별로» 인 거고, 사실상 구조는 크게 두 가지.
 *    ★뭐 이렇게 «능동형이냐, 할 일을 내려주는 거냐» 그런 식으로」
 *
 *  ★그래서 둘을 다르게 다룬다 —
 *      구조는 «닫아» 둔다. 셋째를 만들려면 「일을 어떻게 만나나」 가 또 갈려야 한다.
 *              화면을 새로 짜는 일이라 함부로 늘리지 않는다.
 *      테마는 «열어» 둔다. 하나 늘리는 것이 «여기 한 줄 + CSS 한 장» 이면 된다.
 *              값·클래스·이름이 다 이 표에서 나오므로, 화면 코드는 손대지 않는다.
 *
 *  ★★모바일은 «동일하다» — 2026-09-06 대표
 *
 *  대표: 「팀jpk.com 들어가면 관리 ERP 뜨고, 그 안에서 구조가 우리가 «현대식» 으로 보는 거 있잖아.
 *         그게 우리 «기존에 만들어놨던 JPK워크» — 그게 현대식이고, «고전식» 은 지금 이 방식이야.
 *         ★근데 «모바일은 동일하다». 웹만 다른 거야 웹만.
 *         웹은 구조가 다르고 모바일은 구조랑 이런 거 다 똑같고」
 *
 *  ★그래서 방식은 «웹의 것» 이다. 폰에서는 어느 방식이든 같은 화면을 본다 —
 *    할 일 · 조회 · 등록 · 설정 넷(lib/erp/화면규격.ts 의 뿌리탭).
 *  ★이건 테마와 «같은 결» 의 규칙이다: 테마도 769px 위에서만 산다.
 *    폰은 하나다 — 구조도 하나, 결도 하나.
 *
 *  ★★가르는 잣대 — 처음에 「주소가 바뀌나」 로 적었는데 «그건 틀렸다»(코덱스 H-20260906-001).
 *    테마도 ?테마=요즘 으로 주소를 바꾼다. 주소는 «결과» 지 잣대가 아니다.
 *
 *    ★잣대는 「마크업이 같나」 다 —
 *        같은 JSX 가 그리면 «테마» (CSS 한 장이면 끝난다)
 *        화면을 새로 짜야 하면 «방식»
 *    ★이건 «재진다» — 왼쪽 메뉴 수 · 표 줄 수 · 탭 수 · 상세 유무가 같으면 테마다.
 *      고전 ↔ 요즘은 둘 다 8 · 9 · 1 · true 로 같다.
 *
 *  ── ★무엇이 테마인가 (안 바뀐 규칙)
 *  테마는 «같은 원자를 다르게 그리는 것» 이다. 다른 자료를 보여 주는 것이 아니다.
 *  그래서 자료 통로는 lib/erp/목록.ts 하나이고, 테마는 그 앞에 선다.
 *  ★경계는 docs/테마-규격.md 가 정본이다 — 무엇을 바꿔도 되고 무엇은 못 바꾸나.
 *
 *  ★★「보안」 은 테마도 방식도 아니다. 둘 다 «가리는» 것이지 «막는» 것이 아니다.
 *    외부 사람에게 안 보여야 하는 것은 «서버가 안 보내야» 한다. 열을 숨기는 것으로는 안 막힌다.
 */

/** ★방식 — 누르면 «구조» 가 바뀐다. 그래서 «주소» 를 갖는다 */
export type 방식 = {
  코드: string;
  이름: string;
  길: string;
  /** 이 방식이 «누구를 위한 것» 인가 — 새 방식을 만들 때 여기부터 적는다 */
  무엇: string;
  로그인: boolean;
  /** 설정에서 «고를 수 있나» */
  고를수있나: boolean;
  /** ★이 방식이 테마(껍데기)를 갈아 끼울 수 있나 */
  테마쓰나: boolean;
};

export const 방식들: 방식[] = [
  {
    코드: 'gojeon-erp',
    이름: '고전식',
    /** ★★뿌리다 — 「우리 ERP 는 «레트로 버전이 메인»이야」 (대표 2026-09-08)
     *  ★그래서 길이 '/' 다. /erp-classic 도 그대로 살아 있다(나눠 준 링크가 죽으면 안 된다).
     *    뿌리를 여기로 돌리는 것은 proxy.ts 가 한다 — 화면 파일은 제자리에 둔다. */
    길: '/',
    무엇: '좌측 업무 메뉴 · 우측 목록 · 창 탭 · 아래 상세. 한 화면에서 다 본다 (웹)',
    로그인: false,
    고를수있나: true,
    테마쓰나: true,
  },
  {
    코드: 'harudo',
    이름: '현대식',
    /** ★뿌리를 레트로에게 내주고 «제 주소»로 옮겼다 (2026-09-08).
     *  ★직원 로그인도 이 길에 있다 — 고전은 로그인 없이 보이지만, 실제 자료는 여기로 들어온다. */
    길: '/work',
    무엇: '★기존에 만들어 둔 «JPK워크» — 할 일이 먼저 오고, 찾는 것은 조회로 간다 (웹)',
    로그인: true,
    고를수있나: true,
    /** ★아직 «자기 결» 하나뿐이다. 여기에 고전 결을 씌우려면 화면을 다시 짜야 한다 —
     *  그건 껍데기가 아니라 구조라서, 그날이 오면 «방식» 을 하나 더 만드는 것이 맞다 */
    테마쓰나: false,
  },
  {
    코드: 'maemul',
    이름: '외부영업',
    길: '/maemul',
    무엇: '★외부 영업이 보는 화면 — 서버가 걸러 준 것만 온다(lib/erp/외부영업.ts). 보기만 한다',
    로그인: true,
    고를수있나: false,
    테마쓰나: false,
  },
  {
    코드: 'siyeon',
    이름: '시연',
    길: '/demo',
    무엇: '★밖에 보이는 시연 — «실제 자료를 안 쓴다». 파일 안의 값만 보인다',
    로그인: false,
    고를수있나: false,
    테마쓰나: false,
  },
];

/** ★★테마 — 누르면 «껍데기» 만 바뀐다. 주소가 아니라 «값» 을 갖는다.
 *
 *  ★★테마 하나를 늘리는 법 (대표: 「테마는 되게 여러 가지가 있을 거고」)
 *      ① 아래 표에 «한 줄» 을 더한다 — 코드 · 이름 · 값 · 클래스 · 무엇
 *      ② 그 클래스를 뿌리로 삼는 CSS 를 «한 장» 만든다
 *         (본보기: app/erp-classic/요즘결.css — 색 «이름» 만 갈아 끼우고 규칙 몇 개를 덮는다)
 *      ③ app/erp-classic/결들.css 에 `@import` 한 줄
 *      ★그걸로 끝이다. 화면 코드(page.tsx)는 «손대지 않는다» —
 *        뿌리 클래스도 이 표의 `클래스` 에서 나오고, CSS 도 결들.css 가 모아 들인다.
 *      ★실제로 재 봤다(2026-09-06) — 임시 테마 하나를 이렇게 넣으니 설정에 뜨고 화면에 걸렸다.
 *
 *  ★값은 `?테마=` 와 localStorage 에 그대로 쓰인다 — 사람이 읽는 말이라 한글로 둔다.
 *  ★클래스가 빈 것이 «바탕» 이다(레트로). 바탕은 classic.css 그 자체라 덮을 것이 없다.
 *  ★이름이 두 번 바뀌었다: 요즘 → 현대식 → 요즘.
 *    「현대식」 을 «구조» 가 가져갔기 때문이다. 두 축에 같은 이름을 두면
 *    설정에 「현대식」 이 두 줄에 나온다 — 무엇을 고르는지 알 수 없게 된다.
 *    ★옛 값은 page.tsx 가 받아 준다. 나눠 준 링크가 죽으면 안 된다.
 *
 *  ★★다크는 «아직 못 넣는다» — 색빚부터 갚아야 한다.
 *    테마는 색 «이름» 을 갈아 끼워 도는데, classic.css 에 #rrggbb 가 아직 96개 박혀 있다.
 *    그 96개는 무슨 테마를 씌워도 «안 바뀐다» — 어두운 판 위에 밝은 조각이 남는다.
 *    → scripts/색빚-센다.mjs 가 그 수를 센다. 갚는 만큼 다크가 가까워진다. */
export type 테마 = {
  코드: string;
  이름: string;
  /** `?테마=` · localStorage 에 그대로 쓰인다 */
  값: string;
  /** 뿌리에 걸 클래스. ★빈 것이 «바탕» 이다 */
  클래스: string;
  무엇: string;
};

export const 테마들: 테마[] = [
  {
    코드: 'gojeon',
    이름: '레트로',
    값: '고전',
    클래스: '',
    무엇: '그 시절 업무 프로그램의 결 — 볼록한 단추 · 파인 창 · 촘촘한 격자',
  },
  {
    코드: 'yojeum',
    이름: '요즘',
    값: '요즘',
    클래스: 'cl-요즘',
    무엇: '판을 띄우는 층 — 굳은 선 · 그림자 · 둥근 모서리. 구성은 레트로와 «똑같다»',
  },
];

/** ★기본 테마 — 아무것도 안 골랐을 때. 표의 «첫 줄» 이다 */
export const 기본테마 = 테마들[0].값;

export const 방식찾기 = (코드: string): 방식 | null =>
  방식들.find((t) => t.코드 === 코드) ?? null;

export const 테마찾기 = (값: string): 테마 | null =>
  테마들.find((t) => t.값 === 값) ?? null;
