# WORK-INBOX — Chat R&D → Work 개발 반영용

최종 갱신: 2026-09-16
프로젝트: freepass-admin (구 freepasserp.com 저장소)
목적: ChatGPT 채팅에서 사용자와 확정한 R&D 내용을 Work가 자동 추측하지 않고, GitHub에서 한 곳만 읽고 개발에 반영하도록 만드는 공용 인수인계 문서.

> Work 작업 시작 전 반드시 이 문서와 `AGENTS.md`, `docs/MASTER-v1.md`를 읽는다. 이 문서는 대화 전체를 복사하는 곳이 아니라 **현재 개발에 영향을 주는 최신 결정·시뮬레이션·HOLD·다음 작업**만 요약한다.

---

## 1. 사업 구조 — 가장 먼저 이해할 것

freepass-admin은 단순 상품목록 ERP가 아니다.

```text
공급사 상품 RAW
 → Adapter / Mapping / Validation
 → Canonical Product SSOT
 → 영업자에게 판매 가능한 상품 제공
 → 고객 접수
 → 계약서 / 필수서류 / 인도
 → 실적 생성
 → 영업자 실적 1차 확인
 → 공급사 Cross Check
 → 필요 시 영업자 재확인
 → 최종 정산 확정
 → 공급사 청구 / 계산서
 → 수금
 → 영업채널 지급
 → FreePass Margin
```

핵심 수익구조:
- 공급사로부터 받을 돈
- 영업채널에 줄 돈
- 그 차액 = FreePass 마진

`청구확정`, `계산서`, `수금`, `지급`은 서로 다른 상태다. 하나의 완료값으로 합치지 않는다.

---

## 2. 현재 첫 개발 P0

전체 사업 중 첫 번째 실제 완성 목표는 ADMIN 수직 흐름이다.

```text
Canonical Product
 → Search / Filter
 → Product Detail
 → matched Offer
 → 신규접수
 → Application Snapshot
 → 접수목록
 → 접수상세
 → 계약서 / 필수서류 / 인도 / 취소
```

SALES / WHITE LABEL / 정산 전체 구현을 이 흐름보다 먼저 벌리지 않는다.

---

## 3. ADMIN UI/UX 핵심

Desktop 기본:

```text
상품목록 1/3 | 상품상세 1/3 | 업무패널 1/3
```

역할:
- 목록 = 찾기
- 상세 = 확인
- 업무패널 = 실행

상품 상세에서 `이 상품으로 접수하기`를 누르면 LEFT/CENTER는 유지하고 RIGHT만 신규접수로 전환한다.

접수 중 다른 상품을 목록에서 구경해도 Draft의 접수 대상 상품은 자동 변경되면 안 된다. 접수상품 변경은 명시적 동작이어야 한다.

모바일은 desktop 3열 압축이 아니라 `LIST → DETAIL → WORK` 독립 화면 흐름으로 간다.

UI 시각 구현은 사용자 승인 이미지 revision을 기준으로 한다. 이미지 승인 전 신규 시각 변경을 완료라고 보고하지 않는다.

---

## 4. ERP4 원자 기반 최소 노출 원칙

ERP4 실제 atom 구조에서 확인한 철학을 참고한다. 기존 ERP4 DB/코드/Firebase를 v1 운영 의존성으로 연결하지 않는다.

원자 분류:
- 모델/차 정체
- 제원
- 등록/실차
- 변동값(상태, 주행, 가격 등)
- Offer
- Policy
- 메타/원문/검수정보

화면 원칙:
- 목록에는 찾는 데 필요한 최소 원자만 노출
- 상세에서 확인용 원자 확장
- 원문/source row/내부 메타/검수 근거는 ADMIN 진단 영역으로 제한
- 데이터에 존재한다는 이유만으로 화면에 전부 표시하지 않음
- 부분 차종 매칭을 완성된 트림처럼 꾸미지 않음
- `미확인 ≠ 0 / 불가 / 무제한`

관련 설계 PR: #4

---

## 5. Search Contract — P0

현재 main의 문자열 검색은 Prototype이다. 실제 검색 완료로 간주하지 않는다.

필수 규칙:
- 같은 축 복수값 기본 OR
- 다른 축 기본 AND
- 모델 / 세부모델 / 트림 EXACT-PARTIAL 구분
- 다른 세부모델로 확정된 상품을 PARTIAL에 섞지 않음
- 기간/월대여료/보증금/약정주행거리/Offer-scope Policy는 **같은 Offer 하나**에서 동시에 만족해야 함
- 서로 다른 Offer를 섞어 가짜 조건 생성 금지
- 검색에서 일치한 `offer_id`를 카드 → 상세 → 접수까지 유지
- 보증금 공란은 0원/무보증 검색에 포함 금지

기능 시뮬레이션: PR #4의 `docs/qa/admin-functional-simulation-v1.md`

---

## 6. 접수 Contract — 현재 R&D 결정

현재 설계상 초기 접수 핵심 필드:
- 차량 / 선택 Offer
- 영업채널
- 담당자
- 고객명

연락처 필수 여부 등 아직 확정되지 않은 값은 `DECISION REQUIRED`로 둔다.

접수 저장 시 Snapshot:
- product_id
- explicit product_version 필요
- selected offer_id + 당시 Offer 조건
- 적용 Policy
- vehicle match level
- supplier/source context 필요한 범위
- captured_at

현재 상품 변경이 과거 접수를 변경하면 안 된다.

서버 측 중복 저장 방지(idempotency/submission id) 계약이 필요하다. 버튼 disabled만으로 완료 처리하지 않는다.

접수 상태/사실:
- RECEIVED
- CONTRACTED
- DELIVERED
- CANCELLED
- 별도 체크: 계약서 / 필수서류 / 인도

`차량준비` 단계는 FreePass 업무가 아니므로 만들지 않는다.

---

## 7. 실적 → 청구 → 수금 → 지급 사업 시뮬레이션

인도완료된 접수가 실적 후보가 된다. 중복 Performance 생성 금지.

운영 순서:
1. 영업자 실적 1차 확인
   - 버튼 예: `확인`, `이견 있음`
2. 공급사 Cross Check
   - 버튼 예: `공급사 확인 완료`, `이슈 등록`
3. 공급사 이슈가 영업자 지급액/인정 실적에 영향을 주면 영업자 재확인
   - 버튼 예: `재확인 요청`
   - 영업자: `수용`, `이견 유지`
4. 관리자 최종 확정
   - 버튼 예: `정산 확정`
5. 공급사 청구
   - `청구서 생성`
   - `계산서 처리`
6. 수금
   - `수금 등록`
7. 영업채널 지급
   - `지급 등록`
   - `지급 보류`
8. 금액 표시
   - 청구액 / 수금액 / 미수액
   - 확정 지급액 / 실제 지급액 / 미지급액
   - 우리 마진

부분수금/일부지급을 정상 상태로 지원한다.

상세 R&D는 AI Core PR #6의 사업 운영 모델 및 E2E 시뮬레이션을 참고한다.

---

## 8. 현재 AI Core Gate

통합 기준:
- PR #6 — AI Core Gate 01
- Issue #5 — P0 통합 순서와 HOLD 기준

현재 중요한 Gap:
- main ADMIN UI는 Prototype
- 실제 same-Offer Search Contract 미구현
- Application explicit productVersion 필요
- salesChannelId / assigneeId 계약 필요
- idempotent create 필요
- 독립 Firebase persistence 미검증
- 첫 실제 공급사 Adapter 미연결

병렬 작업:
- PR #2: Snapshot `MULTI_SELECT.value` 참조분리 — 코드/테스트 작성, 실행 검증 대기
- PR #3: UI Profile / review packet — 사용자 exact image 승인 및 evidence gate
- PR #4: Atom Projection + 기능 시뮬레이션
- PR #6: AI Core 통합 통제판

---

## 9. Work 시작 절차

매 작업 시작 시:

1. `git fetch` 후 현재 main과 자신의 branch/PR base 확인
2. `AGENTS.md`
3. `docs/WORK-INBOX.md`
4. `docs/MASTER-v1.md`
5. 해당 작업과 관련된 PR / Issue / AI Core Gate 확인
6. 같은 파일을 다른 AI가 수정 중인지 확인
7. 구현 후 완료 상태를 구분해서 보고

완료 상태 표준:
- DESIGNED
- CODED
- STATIC CHECKED
- TESTED
- PERSISTENCE VERIFIED
- DEPLOYMENT VERIFIED
- USER APPROVED

`CODED`를 `DEPLOYMENT VERIFIED`처럼 보고하지 않는다.

---

## 10. Chat R&D 반영 규칙

이 채팅에서 새로운 사업/UX/상태/버튼 결정이 생기면 ChatGPT는:

1. 대화에서 의미를 정리
2. 기능 시뮬레이션으로 반례 확인
3. 다음 중 맞는 곳에 기록
   - 사업구조 → Business Operating Model
   - 데이터 의미 → MASTER / Domain contract
   - UI 노출 → UI Projection contract
   - 기능 흐름 → Functional simulation
   - 테스트 가능한 규칙 → Regression test
   - 우선순위/충돌 → AI Core Gate
4. `docs/WORK-INBOX.md`의 최신 요약을 갱신
5. 필요 시 Work 대상 Issue/PR에 링크 댓글 남김

따라서 Work는 **이 채팅 자체를 읽을 필요 없이 WORK-INBOX에서 현재 결정을 확인**할 수 있어야 한다.

---

## 11. 지금 Work가 먼저 할 일

1. PR #2 테스트 실제 실행 후 merge 판단
2. same-Offer / EXACT-PARTIAL / unknown!=0 / matched Offer continuity Search 회귀테스트
3. Application Contract에 productVersion + salesChannelId + assigneeId + idempotency 반영
4. 사용자 승인 ADMIN 이미지 기준으로 UI 구현
5. 독립 Firebase 연결 후 실제 저장/재조회/중복방지 검증
6. 승인 공급사 1곳 RAW→Canonical→검색→접수 수직연결

---

## 한 문장

> Work는 기능을 임의로 늘리지 말고, 공급사 원문이 판매 가능한 Canonical 상품이 되어 영업되고, 접수·인도·실적·청구·수금·지급까지 이어지는 사업 흐름 안에서 현재 P0를 구현한다. Chat의 최신 R&D는 이 WORK-INBOX를 통해 전달받는다.

---

## 12. ADMIN 웹 UI·UX R&D 업데이트 — 2026-09-13

상태: **CANDIDATE / USER DIRECTION CONFIRMED / EXACT IMAGE APPROVAL PENDING / IMPLEMENTATION HOLD**

사용자 최신 결정과 AI Core·DevCenter 기반 독립 관점 검토를 반영한 관리자 웹 설계 방향:

- 개발 우선순위는 모바일보다 PC 웹이다.
- 전역 메뉴를 제외한 업무영역은 공통 `목록 1/3 | 상세 1/3 | 현재 업무 1/3` 문법을 사용한다.
- 직원은 왼쪽에서 대상을 고르고, 가운데서 사실·Snapshot을 확인하고, 오른쪽에서 현재 단계의 주 행동 하나를 수행한다.
- 큰 둥근 카드·무거운 전역 상단바·알약형 상태 남발을 피하고 Pretendard, 흰 작업면, 1px 구분선, 44px 입력/버튼, 2줄 목록, 은은한 선택/주의 배경을 사용한다.
- 상품 상세의 기간조건은 압축 가로 표를 폐기한다. 실제 공급사가 제공하는 Offer를 세로 한 행씩 표시하고, 행 전체로 선택한다.
- 각 Offer 행은 같은 Offer의 `기간 | 월 대여료 | 보증금 | 약정주행거리 | 적용 Policy`를 함께 유지한다. 서로 다른 Offer 값 합성은 금지한다.
- 예시 우선기간은 1/6/12/24/36/60개월이지만, DB는 실제 제공 기간을 반복 객체로 수용하며 없는 기간을 생성하지 않는다.
- `공유`와 `접수하기`는 상품 상세 하단에 고정한다.
- 최초 접수 필수값은 정확히 `차량/선택 Offer | 영업채널 | 담당자 | 고객명` 네 개다. 전화번호·주소·생년월일·메모·서류는 최초 접수에 강제하지 않는다.
- 상품 경로 진입 시 차량/Offer를 미리 채우고, 영업채널은 최근값, 담당자는 로그인 사용자를 제안하되 모두 보이고 수정 가능해야 한다.
- 접수 저장은 productVersion·Offer·Policy Snapshot과 idempotency/submission id를 보존해야 한다.
- 접수 화면은 `접수 목록 | 접수 상세 | 진행 업무·이력`, 실적 화면은 `실적 목록 | 실적 상세 | 공급사 대조·확정`을 사용한다.
- 좌측 전역 업무 메뉴는 정확히 `상품 | 접수 | 실적 | 정산` 네 개를 기본으로 한다. `정산` 안에서 `청구 | 지급`을 탭/업무모드로 분리한다. 수금은 청구 상세의 후속 업무로 관리하며, 청구 원장과 지급 원장은 분리한다. 청구확정액/실제수금액/미수액과 지급확정액/실제지급액/미지급액을 합치지 않는다.
- 부분수금·부분지급·취소·조정은 원금액을 덮어쓰지 않고 거래·조정 이력으로 추가한다.
- 목록/상세/업무는 같은 선택 ID를 가리켜야 하며, 뒤로가기에서 검색·필터·스크롤·선택·포커스를 복원한다.
- 로딩·빈값·부분오류·권한없음·동시수정·종료상품·중복저장 상태를 별도로 설계한다.

AI Core 추가 결정 필요:
1. 고객명 최소 저장에 대한 개인정보 보관·권한·마스킹·파기 기준
2. 금액별 VAT 포함/별도 기준
3. 공급사 수금 전 영업채널 지급 허용 정책
4. 부분수금 시 부분지급 정책
5. 마감 후 취소·환수·조정 승인권자

현재 채팅에서 상품/접수, 접수관리, 실적대조, 청구·수금 PC 이미지 후보를 만들었다. 상품 기간조건을 세로 Offer 선택으로 고친 방향은 사용자가 긍정 확인했다. 다만 정확한 최종 이미지 파일/해시를 GitHub 검토 증거로 고정하고 사용자 승인하기 전에는 시각 구현을 시작하지 않는다.

AI Core Gate:
- visual implementation: `HOLD_UNTIL_USER_IMAGE_APPROVAL`
- domain/search/application contract work: 기존 승인 범위에서 계속 가능
- production deploy: `NOT_AUTHORIZED`

---

## 13. ADMIN 시각 기준 확정 — 2026-09-16 · **USER APPROVED**

상태: **DESIGNED / USER APPROVED (시각 방향)** — §12 의 `HOLD_UNTIL_USER_IMAGE_APPROVAL` 은 **해제됐다.**

승인 증거 (WORK-INBOX §12 가 요구한 파일·해시 고정):

```
docs/ui/mockups/admin-product-to-application.html   rev 5
sha256  6d2c26dc171d0c519d8ba5d8f9d7ab4be29cfeb371f6c8e639606863b62cef7d
```

사용자 승인 발언: 「이 느낌으로 가자 이게 맞는거 같다」 · 「이렇게 해야하는게 **모바일로도 할거면** 이게 맞아」 ·
「여기서 디자인 고도화 해나가면 되고 **어차피 중요한건 기능**이니까」

→ 시각은 이 파일을 기준으로 **이어서 다듬는다**. 새 방향을 다시 제안하지 않는다.
→ 우선순위는 **기능**이다. 화면을 예쁘게 만드느라 접수·정산 동작을 미루지 않는다.

### 13-1. §12 를 덮어쓰는 결정

| 항목 | §12 (2026-09-13) | **§13 (2026-09-16) — 이것이 현행** |
|---|---|---|
| 패널 구분 | 1px 구분선 · 큰 둥근 카드 회피 | **패널을 띄운다** — 레일·세 칸이 각자 떠 있는 카드(간격 12, 모서리 16, 층진 그림자). 선으로 나누지 않고 **여백과 면**으로 나눈다 |
| 근거 | — | 모바일을 같이 갈 것이기 때문이다. 3열을 압축하는 것이 아니라 같은 카드가 폰에서 화면 하나가 된다 |

§12 의 나머지(Pretendard, 흰 작업면, 44px 급 입력·버튼, 2줄 목록, 은은한 선택 배경, 알약형 상태 남발 금지)는 **그대로 유효**하다.

### 13-2. 관리자 찾기 — 퀵필터를 두지 않는다

- 대표(2026-09-16): 「관리자는 퀵필터 필요없고 그냥 **검색이랑 세부필터**면 돼 어차피 **검색창에 무보증 이런거 21세 이런거 다 먹히게** 할거니까」
- 관리자 상품 찾기는 **검색창 하나 + 오른쪽 끝 `세부필터`** 다. 화이트라벨의 알약 한 줄은 **손님 화면의 것**이고 관리자에는 두지 않는다.
- 검색어는 **조건을 먹는다**: 무보증 · 만21세 · 카드결제 · 후불 · 분납 · 하이브리드/디젤/가솔린 · N개월 · 월 N만원 이하 · 공급사명.
- ★읽어낸 조건은 **반드시 되돌려 보여 준다**(점선 토큰). 안 보여 주면 직원은 무엇을 걸었는지 모른 채 결과만 줄어드는 것을 보고, 그때부터 화면의 숫자를 안 믿는다.
- 검색어에서 읽은 조건은 세부필터 시트에서 **잠긴 상태**로 보인다 — 끄려면 검색창에서 지운다. 한 조건을 끄는 문이 두 개면 어느 쪽이 진짜인지 모른다.

### 13-3. 세부필터 — 두 칸 시트

fp4 화이트라벨 실물(`components/shop/ShopFilterSheet.tsx`)의 확정 규격을 그대로 쓴다.

- **왼쪽 축 지도 · 오른쪽 값.** 위아래로만 쌓지 않는다 — 밑에 무슨 축이 더 있는지 안 보인다.
- 두 칸은 **따로 구른다.**
- **적용·취소 단추를 두지 않는다.** 고르는 즉시 반영되고 바닥은 `N건 보기`로 결과 수만 말한다.
- 건수는 **그 축을 뺀 나머지 조건**으로 센다(교차 집계). **0건이 되는 값은 세우지 않는다.**

★`EMAIL-RND-CONSOLIDATED.md` 의 「Draft / 닫기 / 적용」과 충돌한다. **fp4 실물이 더 나중이고 이유가 붙어 있어** 그쪽을 따랐다
(취소 단추를 두면 화면의 목록과 시트 안의 선택이 갈려, 닫기 전까지 어느 쪽이 진짜인지 알 수 없다).

### 13-4. 사진

- **사진 위에는 아무것도 얹지 않는다.** 차번도 안 얹는다 — 사진 밝기가 제각각이라 얹은 글자는 어떤 사진에서는 안 읽힌다.
- 신원칩(공급사·확정도)만 **우하단**에 어두운 유리로.
- **사진만 둥글게, 글자는 그냥 밑에.** 카드를 나누는 것은 선이 아니라 여백이다.
- 사진 없는 차가 실측 28% 다 — 네모 빈 상자 대신 둥근 **「사진 준비 중」**.
- ★**사진이 있다고 원자가 있는 것이 아니다.** 사진은 있는데 색상 값이 없는 상품이 실제로 있다. 그때 색상은 `미확인`이다.

### 13-5. 다음 — 기능

시각이 잠겼으므로 남은 것은 전부 **동작**이다.

1. 저장 — `src/ports/repositories.ts` 문 뒤에 실제 저장소를 붙이고 저장·재조회·중복방지를 **실행으로** 확인한다 (독립 Firestore. RTDB 는 폐기 정책상 금지)
2. API — 접수 생성에 `submissionId` 기반 idempotency 를 서버에서 건다
3. 화면 — 승인된 rev 5 대로 ADMIN 수직 한 줄을 실제 데이터로 잇는다
4. 실적 → 청구 → 수금 → 지급 (§7 순서 그대로)
5. 승인 공급사 1곳 RAW → Canonical → 검색 → 접수 수직연결

---

## 14. ★★★디자인 확정 — 2026-09-18 · **굳혔다. 이제 배열만 바꾼다**

대표(2026-09-18, `localhost:3000` 을 보며):
「그냥 이거로 확정하자」 · 「야 디자인 이거로 쓰자 그냥 심플하게」 · 「이거로 굳히는 거야, **여기서 배열만 바꾸고** 가는 거야」
「**나머지 거 그냥 참고용으로만** 하고 이거로 확정 짓고」 · 「그냥 지친다… 자꾸 바뀌고」

### 14-1. 규칙 다섯 — 이것이 정본이다

| # | 규칙 | 대표 말 |
|---|---|---|
| ① | **선은 딱 두 곳 — 판 박스 · 검색창.** 나머지는 선 없는 박스. 가르는 것은 면의 밝기와 사이 | 「라인 있는 건 패널 박스랑 검색 말고는 다 없음」 |
| ② | **라운드는 4 하나** (전에는 5~14px 열 가지였다) | 「라운드만 4로 해 주라」 |
| ③ | **단추는 글자만** — 평소엔 선도 면도 없다. 손을 대면 옅은 면 | 「버튼은 라인 없이 그냥 텍스트로만」 |
| ④ | **고른 것·강조는 색으로만** — 켜진 탭·필터·완료 = **프리패스 네이비 면**(`#1B2A4A`), 고른 카드 = 옅은 남색 면(`#e6ecf5`), 가장 센 강조(선택 Offer) = 남색 박스. 테두리·고리·그림자로 강조하지 않는다 | 「선택된 거나 강조해야 할 건 색깔로만 위계」 · 「너무 쨍한 거 없애고 우리 고유 남색」 |
| ⑤ | **판·목록·표는 박스** — 회색 바탕 위 흰 판(선 있음), 판 위 옅은 회색 카드(선 없음). 표도 칸마다 박스 | 「다 카드형 카드버튼형으로 통일하고 표도」 · 「패널 나눠 놓은 것도 카드형으로」 · 「박스에 담아 넣자」 |

| ⑥ | **글씨는 세 단뿐 — 제목 18 · 메인 14 · 보조 12.** 큰 금액도 제목 단(네 번째 크기 없음) | 「제목글 메인글 보조글로만 3위계」 · 「통상적인 범위로」 |
| ⑦ | **단추·뱃지 규격은 셋 — 뱃지 22 · 단추 33 · 주 단추 44.** 높이를 정하고 글자를 가운데에 세운다 | 「표는 크기에 맞춰서 버튼 뱃지」 |
| ⑧ | **폰은 판을 한 장씩** — 판 탭(상품·상세·접수)은 얼굴 띠 밑에 붙고, **메뉴 넷은 아래 띠 아이콘**(§14-8). 차를 누르면 상세, 접수하기는 접수로 넘어간다. 폰 입력칸만 16px(iOS 확대 막기) | 「모바일 웹 적용해봐」 · 「모바일은 버튼이 하단바에 4개가 아이콘으로」 |
| ⑨ | **가로로 굴리지 않는다** — 단추·칩 줄은 넘긴다. ★예외 하나: 폰의 계약기간(⑪) | — |
| ⑩ | **위 띠 = 프리패스 CI/BI** — 띠는 CI 남색 `#1B2A4A`, 마크는 `icon.svg` 기하 그대로(띠 위라 흰 네모 + 남색 체크), 워드마크 Exo 2 「freepass」600 ·「admin」300. 지금 있는 메뉴 = 밝은 남색 면 + 스카이 `#9EC5F3` 글자 | 「freepass admin 은 우리 CI 규격에 맞춰서 BI 규격으로」 · 「상단바도 남색이랑 색깔 잘 활용」 |
| ⑪ | **계약기간 = 카드 단추** (기간 · 월 대여료 · 보증금/주행이 한 장). 고르기(카드)와 접수(주 단추 하나)를 뗀다. **폰에서는 좌우로 흐르고, 접수 단추와 함께 화면 아래에 붙는다** | 「계약기간은 모바일에서는 좌우로 흐르게 … 어떤 차인지 확인하고 기간을 선택해서 접수한다」 |

★네이비 정본 = freepasserp4 `app/globals.css:37` 「Brand — 프리패스 네이비」 `#1B2A4A`(깊이 `#0F1B35`). `#14263F` 는 유니오토모빌 색이다.
★33 이 홀수인 까닭 — 한글 획 높이가 홀수(메인 13px)라 짝수 상자에서는 위아래가 1px 갈린다. 잉크로 재서 골랐다(단추 18곳 중 14곳 위아래 같은 px · 반 픽셀 0곳).

구현 = `src/app/globals.css` 맨 끝 「대표 확정 2026-09-18」 덩이들 + 디자인 부품 `src/app/_design/`(Brand · TopMenu · OfferPicker).
★CI 정본 = freepasserp4 `app/globals.css:4`(색) · `public/icon.svg`(마크) · `components/sign/sign.css`(반전·워드마크 규격). 고칠 일이 생기면 거기와 같이. 토큰 `--r:4px` · `--바닥` · `--판` · `--박스` · `--박스손` · `--고름` · `--켜짐` · `--선`.
토큰 `--글제목:18` · `--글메인:14` · `--글보조:12`. 글꼴 = Inter/Pretendard. 판 셋 = 상품 목록 | 상품 상세 | 접수 목록.

### 14-0. ★★★화면 = 같은 판의 «배열»일 뿐이다 (대표 2026-09-18)

> 「상세패널은 늘 동일하게 있는 거고, 공통으로 쓰는 게 접수 페이지에 있을 뿐이고」 · 「상품찾기도 그게 좁아진 거뿐이고」
> 「그냥 **각 페이지별로 배열을 어떻게 했나**의 뿐이고」

- 판(부품)은 정해져 있다 — **목록 판 · 상세 판 · 업무 판**, 목록은 **목록 한 줄**(§14-1-2) 하나.
- 메뉴마다 다른 것은 «어느 판을 몇 칸 폭으로 어느 차례에 두나»뿐이다. 판의 모양·글자·색은 어디서나 같다.
- ⇒ 새 화면(정산관리 · 전자계약 · 앞으로 생길 것)은 **새로 그리지 않고 같은 판을 배열**해 짓는다.

### 14-0-1. ★★★절대 법칙 — 위 메뉴 말고는 어디로도 쪽을 안 옮긴다 (대표 2026-09-18)

> 「어떤 페이지에서도 상단에 버튼 누르지 않는 이상 다른 페이지로 가지 않아.. **절대 법칙이다**」

- 쪽을 바꾸는 것은 **위 띠 메뉴 넷뿐**이다. 그 밖의 모든 누름은 «그 쪽 안의 판»만 바꾼다.
- 계약접수: 「이 상품 접수하기」·「+ 신규접수」 → 오른쪽 판 = 신규 접수(`?w=new`) · 접수 줄 → 오른쪽 판 = 접수 상세(`?ic=코드`) · 「목록」 → 접수 목록.
- 접수 저장 뒤에도 계약접수에 머문다(기능 세션: 저장 후 `/intake?ic=…&created=1`).
- 옛 주소(`/intake/new` · `/intake/[code]` · `/products/[id]`)로 들어와도 같은 뜻의 «판 주소»로 넘긴다.
- ⚠ ERP5 쓰기가 켜져 있다 — 화면의 저장·계약서·인도·취소는 운영 원장에 바로 들어간다. 모양 확인 때 누르지 않는다.

### 14-0-2. 뱃지는 두 가지 — 화이트라벨 규격 · 아이콘 + 글자 (대표 2026-09-18 「뱃지 규격이 왜 다르지? 화이트라벨 거 갖고 와」)

정본 = freepasserp4 `components/shop/shop-ui.tsx`(StateChip · PerkMark) + erp4 집 규칙 「박스 뱃지 쓰지 말고 아이콘 텍스트로, 모든 곳에서」(2026-08-28).
부품 = `src/app/_design/Badges.tsx` · 아이콘 = `_design/Icon.tsx`(erp4 lucide 모양 그대로).
- ① **신원 칩**(상품구분 · 출고상태 · 할 일) = 옅은 면 + 아이콘 + 12px, 테두리 없음 · 22px. 그림은 갈래: 출고가능·즉시출고 ○✓(초록) · 계약중·협의 ◷ · 불가 ⊘ · 상품구분 태그.
- ② **조건 표시**(심사 · 혜택) = **면 없이 아이콘 + 굵은 먹색 글자**(색은 아이콘에만). 무심사 = 초록 방패 · 신용조회·소득확인 = 흐린 방패(손님이 «해야 할 일») · 혜택 = 남색 ✓.
- ⓘ 제미나이는 네 모양(채움·테두리·20px·경고)을 권했으나 버렸다 — 테두리는 「선 없음」 위반, 모양 넷은 「규격이 왜 다르냐」의 되풀이.

### 14-0-3. 상품 상세(요약) 차례 — 대표 A 안 (2026-09-18)

사진 → 이름 · (차번 · 공급사) · 배차상태 → 혜택(✓) → 기간 단추 → **값 한 줄**(월 | 보증금 | 선납 | 주행) → 이 상품 접수하기(계약접수에서만).
- 보증금·주행이 두 번 서던 요약 네 칸은 걷었다. 공급사는 이름 밑, 차종 매칭은 상세정보(확정 전일 때만 이름 밑 한 줄).
- 값 한 줄에서 보증금·선납·주행은 빼지 않는다(제미나이: 월 값만 보이면 오접수 사고).

### 14-1-1. 메뉴마다 제 일에 특화 (대표 2026-09-18)

> 「상품찾기는 상품 찾고 상세 화면 보는 거에 특화, 접수는 접수 특화, 정산은 정산 특화, 계약은 전자계약 날리는 거에 특화」
> 「이게 우리 메인인데 이거는 접수 화면이고」 · 「상품찾기는 상품목록 패널 + 상품상세 패널만」 · 「상품목록 2개 패널 사이즈, 상품목록은 그대로」

| 메뉴 | 판 | 주소 |
|---|---|---|
| **계약접수 = 메인** (첫 화면) | 판 셋 — 상품 목록 \| 상품 상세 \| 접수 목록 | `/intake` (`/` → 여기) |
| 상품찾기 | 판 둘 — 상품 목록(판 두 개 폭) \| 상품 상세. 카드는 그대로, 한 줄에 여럿 | `/products` |
| 정산관리 | 판 셋 — 묶음(공급사 청구 / 영업채널 지급 · ‹ 달 › · 청구월 미정) \| 실적 줄(+ 환수 줄 · 끊김 · 받은 몫) \| 접수 상세(계약접수와 같은 판) | `/settlement?tab=&month=&g=&ic=` |
| 전자계약 | 전자계약 보내기 특화 — 다음 차례 | `/esign` |

★상품 목록·상세는 **한 부품**(`src/app/products/workspace.tsx`)이다 — 두 메뉴가 따로 지으면 같은 차가 두 화면에서 다르게 보인다.
★★**상품 상세 판은 «늘 같다»** — 대표 2026-09-18 「상세패널은 늘 동일하게 있는 거고, 공통으로 쓰는 게 접수 페이지에 있을 뿐이고」.
  상품찾기와 계약접수가 «같은 상세 판»을 같은 자리(셋째 칸)에 쓴다. 계약접수는 그 옆에 접수 목록 판이 하나 더 붙을 뿐이다.
  ⇒ 상세를 화면마다 따로 짓지 않는다. 고칠 때는 한 곳(workspace 상세 · `_design/DetailTabs` · `_design/OfferPicker`)만 고친다.
ⓘ 접수 줄 전부(보기·거름)는 `/intake/list` — 메인 오른쪽 판의 「전부 보기」가 그리로 간다.

### 14-1-2. 목록 한 줄 규격 — 모든 목록이 이것 하나 (대표 2026-09-18 「목록 규격 통일해야 할 듯」)

기준 = 확정 목업의 상품 카드. 부품 = `src/app/_design/ListRow.tsx` · 옷 = globals.css 「목록 한 줄 규격」.
```
[사진 82×64]  제목 (메인 14 굵게) ················ 뱃지 (보조 12)
              곁 정보 (보조 12 회색 · 한 줄)
              값 (메인 14 굵게) ···················· 곁값 (보조 12)
```
- 높이 88 고정 — 사진이 있든 없든 같다. 사진 없는 목록(접수·실적)은 사진 칸만 빠진다.
- 뱃지 `act`(할 일이 있다) = 옅은 남색 면 + 남색 글자. 고른 줄 = 옅은 남색 면. 선 없음.
- 쓰는 곳: 상품 목록 · 접수 목록(메인 오른쪽) · 접수 전체(`/intake/list`) · 정산관리 묶음·실적 줄. **새 목록도 이 줄을 쓴다.**
- ★**사진 없는 목록은 사진 자리에 «상태 칸»**(64×64 · 그림 + 한 낱말 · 대표 2026-09-18 「썸네일 아이콘 하나 해서 그 목록의 상태값 · 직관적으로」).
  색은 면에만: 남색 = 진행 · 초록 = 끝 · 붉음 = 멈춤/위험 · 회색 = 대기/취소 · 호박 = 보류. 부품 = `ListRow` `status` · `StatusTile`.
  접수: 접수 · 인도 · 미완료 · 분납 · 완납 · 취소 / 정산 묶음: 완료 · n/m · 대기 · 미정 / 실적 줄: 접수 · 청구(통보) · 정정 · 확인 · 수금(지급) · 보류 · 끊김 · 환수

### 14-3. ★★★하단바 규격 — 하단바를 쓰는 곳은 모두 이것 하나 (대표 2026-09-18)

> 「하단바 규격도 잡아서 하단바 쓰는 곳은 동일하게」 · 「메인 버튼과 비율은 메인이 7 보조가 3 · 메인 버튼이 우측」
> 「버튼들이 상황에 맞게 움직여야지 — 신규 접수를 누르면 그 버튼이 취소랑 옆에 접수 버튼이 나온다든지」

```
┌──────────────────────────────────────────┐
│ [  보조 3  ]  [         주 단추 7         ] │   높이 44 · 보조 = 회색 면(왼쪽) · 주 = 남색(오른쪽)
└──────────────────────────────────────────┘
```
- 판 바닥에 붙는다(흰 면 · 선 없음). 보조가 없으면 주가 한 줄을 다 쓴다. 폰은 아래 메뉴 띠 바로 위.
- ★**바는 판의 상황을 따라 바뀐다** — 같은 자리에서 단추만 갈린다:

| 판 · 상황 | 보조 | 주 |
|---|---|---|
| 상품 상세 (계약접수) | 공유 | 이 상품 접수하기 |
| 상품 상세 (상품찾기) | 공유 | — |
| 접수 목록 | — | + 신규 접수 |
| 신규 접수 | 취소 | 접수 저장 |
| 접수 상세 | 목록 | + 신규 접수 |
| 접수 상세 (정산관리) | 목록 (한 줄 다) | — |
| 세부검색 판 | 초기화(걸린 조건이 있을 때) | N대 보기 |

- ★**자리도 하나** — 하단바는 «판의 마지막 자식»이다. 모든 판에서 같은 줄(PC: 판 안쪽 바닥 · 폰: 아래 메뉴 띠 바로 위)에 선다.
  내용이 짧으면 바닥까지 밀리고, 길면 바닥에 붙는다. 상세 판은 탭(요약·상세정보) «밖»이라 어느 탭에서나 선다.
  바에는 [보조][주] 한 줄만 — 기간 · 값 한 줄 같은 내용은 본문에 둔다(구른다). 「하단 고정 바는 동일하게 유지해 줘야지」
- 판 머리 오른쪽 위에 단추를 두지 않는다(앞서 「공유」 · 「+ 신규접수」 · 「목록」이 거기 있었다 — 전부 바로 옮겼다).
- 부품 = globals.css 「하단바 규격」 `.dz-bar` · `.dz-bar-go` · `.dz-bar-sub` · 주 단추는 `.primary`.

### 14-4. 틀고정 — 무엇이 서 있나 (대표 2026-09-18)

- **목록 판**: 머리 · 검색창 · 퀵 단추가 서 있고 목록만 구른다. **쪽 나눔 없음 — 목록은 쭉 구른다**(「스크롤이 쭉쭉쭉」).
- **상세 판**: 판 전체가 구른다. **서 있는 것은 하단바(접수 덩이) 하나뿐**(「세부 페이지에는 접수 버튼 빼고는 틀고정 필요 없지」).
- 폰: 판 탭은 얼굴 띠 밑에, 하단바는 아래 메뉴 띠 위에 붙는다.

### 14-5. 상세정보 — erp4 «읽는 차례»로 (대표 2026-09-18 「상세 페이지 정리 좀 하자」)

차량 → 대여료(기간별 요금 한 표 + 추가 요금) → 운전자 → 보험(항목 │ 보장한도 │ 면책금 한 표 · 제목 옆 「보험료 포함」) → 계약 조건(조항 번호) → 영업 전용(내부) → 기타(접힘).
- 원자는 기능 쪽 `productSections` 그대로 — 화면은 «어느 칸을 어느 자리에»만 정한다. 부품 = `_design/ProductInfo.tsx`.
- 구역 머리 = 아이콘 하나 + 이름(칸 안에는 그림 없음). 값에 단위(연 30,000km · 만 26세 · 30일).
- 빈 칸은 버리지 않는다 — 둘까지는 흐린 「—」로 제자리, 셋 이상은 구역 끝 「빈 칸 N개」 안에 접는다.
- 정산 줄(접수 상세)은 `settlementSections` 구역 그대로 + 늘 보일 칸(pinned)은 「정산 진행」으로 금액 밑에.

### 14-6. 글 — 어정쩡한 영문 없음 (대표 2026-09-18 「어정쩡한 영문들 빼자」)

- 판 머리 영문 머리말(PRODUCT · DETAIL · WORK · APPLICATION)을 쓰지 않는다. 사진 위 「SSOT」 없음.
- 원본 칸 이름(injury_deductible …)을 그대로 세우지 않는다 — 이름을 붙이거나(`ProductInfo` 옛이름) 세우지 않는다.
- 영문은 로고(freepass admin) · 단위(km · cc · kWh)만.

### 14-7. 세부검색 = 화이트라벨 두 칸 조건판 (대표 2026-09-18)

왼쪽 축 지도 │ 오른쪽 값 + 건수(교차 집계). 0 도 줄은 선다. 여러 값 고르기(같은 축 «또는», 축끼리 «이면서»). 적용/취소 없음 — 고르면 바로 걸리고 바닥은 「N대 보기」. 부품 = `_design/FilterSheet.tsx`.

### 14-7-1. 판 머리 — 당근 결 (대표 2026-09-18 「패널 헤드 이쁘게 정리 좀 · 당근 느낌 나게끔, 색깔이 그렇다는 게 아니라」)

굵은 제목 하나(18 · 800) + **바로 옆 흐린 숫자**(「상품 목록 675대」). 숫자를 상자에 담아 오른쪽 끝으로 밀지 않는다. 머리에는 그 밖의 것을 두지 않는다(단추는 하단바).

### 14-8. 폰 아래 띠 — 메뉴 넷 아이콘 (대표 2026-09-18 「모바일은 버튼이 하단바에 4개가 아이콘으로」)

위 띠 = CI 얼굴만. 아래 띠 = 메뉴 넷(아이콘 + 이름 · 남색 띠 · 지금 있는 곳 스카이). 웹은 위 띠 글자 메뉴 그대로. 부품 = `_design/Brand.tsx` TopMenu 하나(두 벌로 가르지 않는다).

### 14-9. ★★★하나로 통일한 항목 — 이 표가 규격 정본 (대표 2026-09-18 「하나로 통일할 항목들을 만들어봐」)

> 「검색창이랑 버튼이랑 규격이 같았으면 · 사이즈 규격이 상하나 이런 것들 · 규격이 다 중구난방이고」
> 「목록카드 색깔도 위계를 확실하게 · 배경이랑 살짝만 차이 나도」 · 「검색창 안에 돋보기 아이콘도 너무 작고」

| # | 항목 | 규격 (하나뿐) | 토큰 · 부품 |
|---|---|---|---|
| 1 | **높이** | 뱃지 **22** · 컨트롤 **40** — 검색창 · 퀵 단추 · 탭 · 기간 · 하단바 · 입력칸 · 달 넘기기 · 걸음 길 전부 40. 예외 둘: 검색창 «안» 세부검색 32(안쪽 4) · 폰 하단바 44(엄지) | `--컨트롤` |
| 2 | **글** | 제목 18(판 머리 800) · 메인 14 · 보조 12. 줄 높이 16(컨트롤 안) | `--글제목` `--글메인` `--글보조` |
| 3 | **라운드** | 4 하나 | `--r` |
| 4 | **선** | 판 박스 · 검색창 두 곳뿐(`#dfe4e9`). 나머지는 면의 밝기로 가른다 | `--선` |
| 5 | **면 위계** | 바닥 `#eef1f4` < 판 `#fff` < **카드 `#f6f7f9`**(판보다 살짝) < 손 댄 카드 `#eef1f4` < 고른 카드 `#e6ecf5`. 카드 안 뱃지는 카드보다 한 단 짙게(`#eaeef2`), 고른 카드 위에서는 흰 면 | `--바닥` `--판` `--카드` `--카드손` `--고름` |
| 6 | **켜짐 · 주 단추** | 프리패스 남색 `#1B2A4A` 면 + 흰 글자. CI 띠 위 켜짐만 스카이 `#9EC5F3` | `--ci남색` `--ci스카이` |
| 7 | **뜻 색 넷** | 초록 = 끝·좋은 소식 · 붉음 = 멈춤·위험(미완료·정정·끊김·청구월 미정) · 호박 = 보류·내부 · 회색 = 대기·취소. 색은 «면»과 «아이콘»에만 | globals.css |
| 8 | **아이콘** | erp4 lucide 한 벌(획 2). 크기: 뱃지 12 · 조건 14 · 구역 머리 15 · **검색 돋보기 18** · 상태 칸 20 · 폰 메뉴 22 | `_design/Icon.tsx` |
| 9 | **사이** | 판 여백 22(폰 16) · 판 사이 12 · 덩이 사이 12 · 칸 사이 8 · 단추 사이 4 | — |
| 10 | **판 머리** | 굵은 제목 + 옆 흐린 숫자(「상품 목록 675대」). 단추 없음 | §14-7-1 |
| 11 | **검색** | 창 40(돋보기 18 · 안에 세부검색 32) → 밑에 퀵 단추 40 한 줄 | §14-7 · `FilterSheet` |
| 12 | **목록 한 줄** | 88 고정 · 사진 82×64 또는 상태 칸 64×64 · 세 줄(제목·뱃지 / 곁 정보 / 값·곁값) | §14-1-2 · `ListRow` |
| 13 | **뱃지** | 둘뿐 — 신원 칩(옅은 면 + 아이콘 + 글) · 조건 표시(면 없이 색 아이콘 + 굵은 글) | §14-0-2 · `Badges` |
| 14 | **하단바** | 판의 마지막 · 모든 판 같은 줄 · 보조 3 : 주 7(주는 오른쪽) · 상황 따라 단추가 바뀜 | §14-3 · `.dz-bar` |
| 15 | **틀고정** | 목록 머리(머리·검색·퀵) · 하단바 둘뿐 | §14-4 |
| 16 | **상세 구역** | 머리(아이콘 하나 + 이름) + 같은 표(항목 │ 값). 빈 값 「—」 흐리게, 셋 이상이면 접음 | §14-5 · `ProductInfo` `Sections` |
| 17 | **글 말** | 어정쩡한 영문 없음(로고 · 단위만) | §14-6 |
| 18 | **폰** | 판 탭(40) 위 · 메뉴 넷 아이콘 아래 띠 · 판 한 장씩 | §14-8 |

★새 화면 · 새 부품은 이 표에서 고른다 — 표에 없는 높이 · 색 · 라운드를 새로 만들지 않는다. 표를 바꿀 일이 생기면 **표를 먼저 고치고** CSS 를 따라 고친다.

### 14-2. 이제부터

- ★**옷은 안 바꾼다. 바뀌는 것은 «배열»뿐이다** — 무엇을 어느 판·어느 차례에 둘지.
  새 색·새 라운드·선 되살리기·레트로/민트 같은 결 바꾸기를 **제안하지 않는다.**
- ★**나머지 디자인 문서는 전부 «참고용»이다** — 이 문서 §3·§12·§13, `docs/ui/*`, `docs/reviews/ADMIN-DESIGN-REVIEW-*`,
  정산 워크스테이션(`fp-settlement`)의 고전 레트로 규격. 이 절과 어긋나면 **이 절이 맞다.**
- 정산 워크스테이션도 이 옷으로 간다. 옮기기 전까지는 «돌아가는 정산 도구»로 그대로 쓴다.
- ⓘ §13-3 「0건 값은 세우지 않는다」는 관리자 화면에 안 쓴다 — 대표 2026-09-10 「옆에다 0이라고 해줘야지」가 우선. 값은 세우고 숫자만 0.
- 우선은 **기능**이다.
